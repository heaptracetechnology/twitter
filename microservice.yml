commands:
  follow:
    help: Follow an user
    arguments:
      who:
        type: string
  followers:
    help: List an user's followers
    arguments:
      who:
        type: string
  retweet:
    help: Retweet a tweet
    arguments:
      tweet_id:
        type: string
  tweet:
    help: Tweet a message
    arguments:
      message:
        type: string

environment:
  CONSUMER_KEY:
    type: string
  CONSUMER_SECRET:
    type: string
  ACCESS_TOKEN:
    type: string
  ACCESS_TOKEN_SECRET:
    type: string
